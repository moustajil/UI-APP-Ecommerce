import 'package:flutter/material.dart';

class DetailsProduct extends StatelessWidget {
  final data;
  DetailsProduct({super.key, this.data});

  @override
  Widget build(BuildContext context) {
    return ListView(
      children: [],
    );
  }
}
